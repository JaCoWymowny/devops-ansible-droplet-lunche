server {
    listen 80;
    server_name {{ item }};
    root /var/www/my_website/{{ item }};

    access_log /var/log/nginx/{{ item }}_access.log;
    error_log /var/log/nginx/{{ item }}_error.log;

    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }
}